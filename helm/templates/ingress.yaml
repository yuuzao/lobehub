{{- if .Values.lobe.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "lobehub.fullname" . }}
  labels:
    {{- include "lobehub.labels" . | nindent 4 }}
  {{- with .Values.lobe.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.lobe.ingress.className }}
  ingressClassName: {{ .Values.lobe.ingress.className }}
  {{- end }}
  {{- if .Values.lobe.ingress.tls }}
  tls:
    {{- range .Values.lobe.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.lobe.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "lobehub.fullname" $ }}
                port:
                  name: http
          {{- end }}
    {{- end }}
{{- end }}
